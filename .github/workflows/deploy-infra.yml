name: Deployment Agile Chat Infrastructure
on:
  workflow_dispatch:
    inputs:
      reason:
        description: "The reason for running the workflow:"
        required: false
        default: Manual run
      resource_group:
        description: "Enter the Azure resource group Name:"
        required: true
      azure_env_name:
        description: "Enter the Azure Environment Name:"
        required: true
        default: "agile-dev"
      azure_location:
        description: "Select the Azure deployment region:"
        required: true
        type: choice
        default: "australiaeast"
        options:
          - "australiaeast"
          - "australiasoutheast"
      environment:
        description: "Select the Azure environment:"
        required: true
        type: choice
        default: "Development"
        options:
          - Development
          - Test
          - Production
      bicep_params_file:
        description: "Enter the Azure Bicep Parameters filename (e.g., ./infra/dev.parameters.json):"
        required: true
        default: "agile-dev"

#   push:
#     branches:
#       - main

# Set up permissions for deploying with secretless Azure federated credentials
# https://learn.microsoft.com/en-us/azure/developer/github/connect-from-azure?tabs=azure-portal%2Clinux#set-up-azure-login-with-openid-connect-authentication
permissions:
  id-token: write
  contents: read

jobs:
  infra-deploy:
    runs-on: ubuntu-latest
    environment: ${{ github.event.inputs.environment }}

    env:
      AZURE_CLIENT_ID: ${{ vars.AZURE_CLIENT_ID }}
      AZURE_TENANT_ID: ${{ vars.AZURE_TENANT_ID }}
      AZURE_SUBSCRIPTION_ID: ${{ vars.AZURE_SUBSCRIPTION_ID }}
      AZURE_RESOURCE_GROUP: ${{ github.event.inputs.resource_group }}"
      AZURE_ENV_NAME: ${{ github.event.inputs.azure_env_name }}"
      AZURE_LOCATION: ${{ github.event.inputs.azure_location }}"
      AZURE_BICEP_MAIN: "./infra/main.bicep"
      AZURE_BICEP_PARAMS: ${{ github.event.inputs.bicep_params_file }}"
      ENVIRONMENT: ${{ github.event.inputs.environment }} # Sets ENVIRONMENT based on input

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Login to Azure
        uses: azure/login@v2
        with:
          client-id: ${{ env.AZURE_CLIENT_ID }}
          tenant-id: ${{ env.AZURE_TENANT_ID }}
          subscription-id: ${{ env.AZURE_SUBSCRIPTION_ID }}

      - name: Lint and Prep Bicep
        uses: azure/cli@v2
        with:
          azcliversion: 2.64.0
          inlineScript: |
            tdnf install -y icu
            tdnf install -y jq
            az bicep install
            az config set bicep.use_binary_from_path=false && az bicep build -f ${{ env.AZURE_BICEP_MAIN }} --stdout

      - name: Check if Resource Group Exist
        id: check_rsg
        uses: azure/cli@v2
        with:
          azcliversion: 2.64.0
          inlineScript: |
            az group show --name "${{ env.AZURE_RESOURCE_GROUP }}" >/dev/null 2>&1 || echo "not found"

      - name: Create Resource Group (if not exists)
        if: steps.check_rsg.outputs.exit_code == 'failure'
        uses: azure/cli@v2
        with:
          azcliversion: 2.64.0
          inlineScript: |
            az group create --name "${{ env.AZURE_RESOURCE_GROUP }}" --location "${{ env.AZURE_LOCATION }}"

        ##https://github.com/Azure/bicep-deploy
      - name: Deploy Bicep to Azure
        uses: azure/bicep-deploy@v1
        with:
          type: deployment
          operation: create
          name: "${{ env.AZURE_ENV_NAME }}"
          scope: resourceGroup
          resource-group-name: "${{ env.AZURE_RESOURCE_GROUP }}"
          subscription-id: "${{ env.AZURE_SUBSCRIPTION_ID }}"
          template-file: "${{ env.AZURE_BICEP_MAIN }}"
          parameters-file: "${{ env.AZURE_BICEP_PARAMS }}"
